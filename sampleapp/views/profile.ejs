<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end"><a href="/logout"
                class="px-3 py-2 bg-red-500 rounded-md absolute top-5 right-5">Logout</a>
             <a href="/profile/uploadimg"
                class="px-3 py-2 bg-red-500 rounded-md absolute left-5 top-5">Change Profile Photos</a></div>
             <div class="flex gap-6 items-center mt-20 mb-10">
                 <div class="w-20 h-20 rounded-md overflow-hidden">
                        <img class="w-full h-full object-cover rounded-md" src="/images/uploads/<%= user.profileimg %>" alt="profile pic">
                 </div>
        <h3 class="text-3xl mb-5"><span class="font-light">Hello</span>,<%= user.name %>👋</h3>
             </div>  
        <h5 class="mb-5">Create a new Post</h5> 
        <form action="/post" method="POST">
            <textarea placeholder="What's on your mind?"
                class="w-1/3 resize-none block bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none"
                name="content"></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2" type="submit" value="Create a new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5 flex flex-wrap gap-5">
                <% user.posts.reverse().forEach(function(post){ %>    
                  <div class="post w-1/3 p-4 border-2 border-zinc-800 mb-2 rounded-md">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-xl tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
                    <div class="btns flex gap-5">
                        <a href="/like/<%= post._id %>" class="text-blue-500"><%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %></a>
                        <a href="/edit/<%= post._id %>" class="text-zinc-600">Edit</a>
                    </div>
                </div>
                <% })%> 
                
            </div>
        </div>
    </div>
</body>

</html>